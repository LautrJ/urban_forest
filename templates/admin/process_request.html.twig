{% extends 'base.html.twig' %}

{% block title %}Traiter la demande{% endblock %}

{% block body %}
    <div class="process-container">
        <a href="{{ path('app_admin_dashboard') }}" class="back-link">← Retour au dashboard</a>

        <div class="request-details">
            <div class="request-header">
                <h1>Demande de recensement</h1>
                <p style="color: #666; margin: 0;">
                    Soumise le {{ treeRequest.createdAt|date('d/m/Y à H:i') }}
                    par {{ treeRequest.citizen.name }}
                </p>
            </div>

            <div class="details-grid">
                <div class="detail-item">
                    <div class="detail-label">Citoyen</div>
                    <div class="detail-value">
                        <strong>{{ treeRequest.citizen.name }}</strong><br>
                        {{ treeRequest.citizen.email }}
                    </div>
                </div>

                <div class="detail-item">
                    <div class="detail-label">Essence proposée</div>
                    <div class="detail-value">
                        {% if treeRequest.proposedTreeType %}
                            {{ treeRequest.proposedTreeType.commonName }}<br>
                            <small><em>{{ treeRequest.proposedTreeType.scientificName }}</em></small>
                        {% else %}
                            <em>Non précisée - À identifier par l'agent</em>
                        {% endif %}
                    </div>
                </div>

                <div class="detail-item">
                    <div class="detail-label">Âge estimé</div>
                    <div class="detail-value">
                        {% if treeRequest.estimatedAge %}
                            {{ treeRequest.estimatedAge }} ans
                        {% else %}
                            <em>Non précisé</em>
                        {% endif %}
                    </div>
                </div>

                <div class="detail-item">
                    <div class="detail-label">Rendez-vous demandé</div>
                    <div class="detail-value">
                        {% if treeRequest.needsAppointment %}
                            ✅ Oui, le citoyen souhaite un RDV
                        {% else %}
                            ❌ Non nécessaire
                        {% endif %}
                    </div>
                </div>

                <div class="detail-item">
                    <div class="detail-label">Coordonnées GPS</div>
                    <div class="detail-value">
                        Latitude: {{ treeRequest.latitude|number_format(6) }}<br>
                        Longitude: {{ treeRequest.longitude|number_format(6) }}
                    </div>
                </div>
            </div>

            {% if treeRequest.description %}
                <div class="detail-item">
                    <div class="detail-label">Description / Commentaires</div>
                    <div class="detail-value">{{ treeRequest.description }}</div>
                </div>
            {% endif %}

            <div class="map-preview">
                📍 Aperçu carte (à implémenter avec Leaflet)
            </div>
        </div>

        <div class="actions-container">
            <h2>Actions</h2>

            {% if not treeRequest.proposedTreeType %}
                <div class="warning-box">
                    ⚠️ <strong>Attention :</strong> Aucune essence n'a été proposée par le citoyen.
                    Vous devrez identifier l'arbre avant de valider la demande.
                </div>
            {% endif %}

            <div class="action-buttons">
                <form method="post" action="{{ path('app_admin_request_validate', {id: treeRequest.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir valider cette demande et créer l\'arbre ?');">
                    <button type="submit" class="btn btn-validate" {% if not treeRequest.proposedTreeType %}disabled title="Essence requise"{% endif %}>
                        ✅ Valider et créer l'arbre
                    </button>
                </form>

                <form method="post" action="{{ path('app_admin_request_reject', {id: treeRequest.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir refuser cette demande ?');">
                    <button type="submit" class="btn btn-reject">
                        ❌ Refuser la demande
                    </button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}